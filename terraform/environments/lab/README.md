# Terraform Lab Environment

This directory will contain the Terraform code for Sprint 1 — AWS Foundation.

Planned files:

- `versions.tf`
- `providers.tf`
- `variables.tf`
- `main.tf`
- `outputs.tf`
- `terraform.tfvars.example`

> Never commit AWS access keys, secret keys, passwords, VPN PSKs, private keys, or Terraform state containing sensitive data.
